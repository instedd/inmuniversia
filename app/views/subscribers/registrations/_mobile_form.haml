= form_for(@channel, :url => path) do |f|
  .control-group
    .controls
      = f.hidden_field :from_dashboard, :value => from_dashboard
      = f.text_field :address, :class => "span10"
      = f.submit "Ok", :class => "btn btn-small btn-primary"