- vaccines_hash = @vaccines.group_by{|v| v.name[0]}

- content_for :topbar do
  %h1 Vacunas

- content_for :body do
  .row-fluid
    .span12
      %ul.alphabet-list
        - vaccines_hash.keys.sort.each do |starting_letter|
          %section
            %h3
              = starting_letter
            - vaccines_hash[starting_letter].each do |vaccine|
              %li
                = link_to vaccine.name, vaccine